name: test github actions

on: push

jobs: 
    run_push:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - uses: actions/setup-node@v1
              with: 
                node-version: 6
            - run: npm install
            - run: npm start